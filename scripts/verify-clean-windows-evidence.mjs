const stageIds = ["qualify","install","doctor","service","weixin_login","codex_desktop","seven_primitives","weixin_e2e","upgrade","rollback","uninstall"];
const requiredApprovals = ["weixin_login","real_codex_home","hook_trust","desktop_restart","computer_use","real_weixin_send"];
const qualifyingKinds = ["second_computer", "resettable_vm"];
const secretPattern = /(?:bearer\s+[^ ]+|sk-[A-Za-z0-9_-]{8,}|(?:token|secret|password|credential)\s*[:=]\s*[^ <[]+)/iu;
import { readFile } from "node:fs/promises";
import path from "node:path";
import { fileURLToPath } from "node:url";

export function validateCleanWindowsEvidence(value) {
  object(value, "clean Windows evidence"); exact(value, ["approvals","archive","commands","environment","schemaVersion","stages","versions"]);
  if (value.schemaVersion !== 1) throw new Error("Clean Windows evidence schema is unsupported.");
  const environment=object(value.environment,"environment"); exact(environment,["arch","freshProfile","kind","os","priorChat2CodexAbsent","repositoryAbsent"]);
  const qualifying=qualifyingKinds.includes(environment.kind)&&environment.freshProfile===true&&environment.repositoryAbsent===true&&environment.priorChat2CodexAbsent===true&&environment.os==="Windows 11"&&["x64","arm64"].includes(environment.arch);
  const archive=object(value.archive,"archive"); exact(archive,["sha256","size","version"]); if(typeof archive.version!=="string"||!Number.isSafeInteger(archive.size)||archive.size<1||typeof archive.sha256!=="string"||!/[a-f0-9]{64}/u.test(archive.sha256)) throw new Error("Clean Windows archive hash or version is invalid.");
  const versions=object(value.versions,"versions"); exact(versions,["codexCli","codexDesktop","node","npm"]); for(const version of Object.values(versions)) if(typeof version!=="string"||version.length<1||version.length>80) throw new Error("Clean Windows version is invalid.");
  if(!Array.isArray(value.commands)||value.commands.length>256) throw new Error("Clean Windows commands are invalid."); for(const command of value.commands){object(command,"command"); exact(command,["command","exitCode","observedAt"]); if(typeof command.command!=="string"||secretPattern.test(command.command)) throw new Error("Clean Windows command must redact secrets."); if(!Number.isSafeInteger(command.exitCode)) throw new Error("Clean Windows command exit is invalid."); timestamp(command.observedAt);}
  if(!Array.isArray(value.approvals)||value.approvals.length>32) throw new Error("Clean Windows approvals are invalid."); const approvals=new Set(); for(const item of value.approvals){object(item,"approval"); exact(item,["action","approvedAt","approvedBy"]); if(!requiredApprovals.includes(item.action)||item.approvedBy!=="Haoda") throw new Error("Clean Windows approval is invalid."); timestamp(item.approvedAt); approvals.add(item.action);}
  if(!Array.isArray(value.stages)||value.stages.length!==stageIds.length) throw new Error("Clean Windows stages are incomplete."); let directPasses=0; for(const stage of value.stages){object(stage,"stage"); exact(stage,["completedAt","evidence","id","startedAt","verdict"]); if(!stageIds.includes(stage.id)||!["unproven","pass","contradicted"].includes(stage.verdict)||!Array.isArray(stage.evidence)) throw new Error("Clean Windows stage is invalid."); if(stage.verdict==="pass") directPasses+=1; if(stage.startedAt!==null) timestamp(stage.startedAt); if(stage.completedAt!==null) timestamp(stage.completedAt);}
  if(new Set(value.stages.map((item)=>item.id)).size!==stageIds.length) throw new Error("Clean Windows stage identities are incomplete.");
  if(directPasses>0){if(!qualifying) throw new Error("Passing evidence requires a qualifying clean Windows environment."); for(const approval of requiredApprovals) if(!approvals.has(approval)) throw new Error(`Missing clean Windows approval: ${approval}`); if(value.stages.some((stage)=>stage.verdict!=="pass")) throw new Error("Clean Windows passing claim requires every stage.");}
  return { qualifyingEnvironment: qualifying, directPasses };
}
function object(value,label){if(!value||typeof value!=="object"||Array.isArray(value))throw new Error(`${label} must be an object.`);return value;} function exact(value,keys){const actual=Object.keys(value);if(actual.some((key)=>!keys.includes(key))||keys.some((key)=>!actual.includes(key)))throw new Error("Clean Windows evidence fields are invalid.");} function timestamp(value){if(typeof value!=="string"||new Date(value).toISOString()!==value)throw new Error("Clean Windows timestamp is invalid.");}
async function main(){const root=path.resolve(fileURLToPath(new URL("..",import.meta.url)));const relative=process.argv[2];if(!relative||path.isAbsolute(relative))throw new Error("Clean Windows manifest path must be repository-relative.");const file=path.resolve(root,relative);const rel=path.relative(root,file);if(rel===".."||rel.startsWith(`..${path.sep}`)||path.isAbsolute(rel))throw new Error("Clean Windows manifest escapes the repository.");const result=validateCleanWindowsEvidence(JSON.parse(await readFile(file,"utf8")));process.stdout.write(`Clean Windows evidence valid: qualifying=${result.qualifyingEnvironment}; directPasses=${result.directPasses}\n`);}
if(process.argv[1]&&path.resolve(process.argv[1])===path.resolve(fileURLToPath(import.meta.url)))main().catch((error)=>{process.stderr.write(`clean-windows-evidence: ${error instanceof Error?error.message:String(error)}\n`);process.exitCode=1;});
